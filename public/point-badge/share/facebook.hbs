<html>
    <head>
        <meta property="og:url"           content="{{base_url}}/point-badge?point={{point}}&total={{total}}&streak={{streak}}&theme={{theme}}&confetti={{confetti}}&share=facebook" />
        <meta property="og:type"          content="website" />
        <meta property="og:title"         content="Look How Many Points I Have" />
        <meta property="og:description"   content="I have shared a post and earned points. You too share a post to #SaveSoil. http://savesoil.com" />
        <meta property="og:image"         content="{{base_url}}/point-badge?point={{point}}&total={{total}}&streak={{streak}}&theme={{theme}}&confetti={{confetti}}" />
    </head>
    <body>
        <img src="{{base_url}}/point-badge?point={{point}}&total={{total}}&streak={{streak}}&theme={{theme}}&confetti={{confetti}}"></img>
        <a href="{{base_url}}">SaveSoil</a>
    </body>
</html>